{% include "components/header.njk" %}
<main role="main">
	<div class="fr-container fr-mt-4w fr-mb-4w">
    	<div class="fr-grid-row fr-grid-row--gutters fr-grid-row--center">
    		<div class="fr-col-8">
				<h1>{{ title }}</h1>
				{{ content | safe }}
				{% set curThemeNum = 0 %}
				{% for c in collections.criteriaAndTests %}
					{% set crit = c.criterion %}
					{% set critNum = c.critNum %}
					{% set themeNum = c.themeNum %}
					{% set tests = c.tests %}

					{% if themeNum > 1 and themeNum !== curThemeNum %}
						</ul>
					{% endif %}

					{% if themeNum !== curThemeNum %}
						{% set curThemeNum = themeNum %}
						<h2 id="{{ themeNum }}">{{ themeNum }}.&nbsp;{{ themes[themeNum].title }} <a class="direct-link" href="#{{ themeNum }}">#</a></h2>
						<ul>
					{% endif %}

					<li>
						<h3 id="{{ critNum }}">Critère {{ critNum }} <a class="direct-link" href="#{{ critNum }}">#</a></h3>
						{{ crit.templateContent | safe }}
						<ul>
						{% for t in tests %}
							<li id="{{ t.testSlug }}">
								<h4>Test {{ t.testFullNum }} <a class="direct-link" href="#{{ t.testSlug }}">#</a></h4>
								{{ t.test.templateContent | safe }}
							</li>
						{% endfor %}
						</ul>
					</li>
				{% endfor %}
				</ul>
			</div>
		</div>
	</div>
</main>
{% include "components/footer.njk" %}




